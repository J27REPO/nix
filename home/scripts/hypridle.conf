general {
    ignore_dbus_inhibit = false
    lock_cmd = notify-send "🔒 Locking screen..."
    before_sleep_cmd = notify-send "😴 Preparing to sleep..."
    after_sleep_cmd = notify-send "⏰ Good morning!"
}

listener {
    timeout = 900
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 1200
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}